name: shafique
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  iconsax: ^0.0.8
  intl: ^0.18.1
  get: ^4.6.5
  http: ^1.1.0
  shimmer: ^2.0.0
  google_fonts: any
  cached_network_image: ^3.2.3
  flutter_smart_dialog: ^4.5.5


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0  


# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/images/

  # fonts:
  #   - family: poppins
  #     fonts:
  #       - asset: assets/fonts/poppins.ttf